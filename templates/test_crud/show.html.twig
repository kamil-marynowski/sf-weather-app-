{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block body %}
    <h1>Test</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ test.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ test.description }}</td>
            </tr>
            <tr>
                <th>Inter</th>
                <td>{{ test.inter }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{{ test.datetime ? test.datetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_crud_index') }}">back to list</a>

    <a href="{{ path('app_test_crud_edit', {'id': test.id}) }}">edit</a>

    {{ include('test_crud/_delete_form.html.twig') }}
{% endblock %}
