{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}
    <div class="row">
        {% for slot in slots %}
            <div class="col">
                <div class="card">
                    <div class="card-header">
                        <h2>{{ slot.datetime|date('d.m.Y') }}</h2>
                    </div>
                    <div class="card-body">
                        <table class="table table-bordered">
                            <tr>
                                <td colspan="2">
                                    <span class="{{ slot.iconClass }}"></span>
                                </td>
                            </tr>
                            <tr>
                                <td>
                                    Temp.
                                </td>
                                <td>
                                    {{ slot.temperature }} ℃
                                </td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
